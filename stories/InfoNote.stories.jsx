import InfoNote from "../src/components/mui/InfoNote";

export default { title: "MUI/Feedback/InfoNote", component: InfoNote };

export const Default = {
  args: { message: "Prices shown exclude tax. Tax is applied at checkout." }
};

